import * as React from 'react';
// import jQuery from 'jquery'
// (window as any).jQuery = jQuery
// //@ts-ignore
// import bootstrap from '../../node_modules/bootstrap/dist/js/bootstrap.bundle.js'
// false && console.log(jQuery, bootstrap, jQuery)
// import 'whatwg-fetch'

// import { Router } from 'react-router';
// import { createHashHistory } from 'history';
// import { ConnectedRouter } from 'connected-react-router';
// import { Provider } from 'react-redux';
// import { ThemeProvider } from 'emotion-theming';
// import { layoutInitialState } from '../store/layout';
// import { listRecordTypesInitialState } from '../store/listRecordTypes';
// import { recordViewInitialState, fetchRecord } from '../store/recordView';
// import { ApplicationState } from '../store';
// import { searchInitialState } from '../store/search';
// import configureStore from '../configureStore';
// import { light } from '../styles/theme';
// import { RecordView } from '../pages/recordView/recordView';
// import { mount } from 'enzyme';
describe('ErrorComponent', () => {
  xit('trying to replicate main', () => {
   expect(1).toBe(1)

  });

//   xit('trying to replicate main', () => {
//     const history = createHashHistory()

//     const initialState: ApplicationState = { layout: layoutInitialState, listRecordTypes: listRecordTypesInitialState, recordView: recordViewInitialState, router: { location: history.location, action: "REPLACE" }, search: searchInitialState }

//     const store = configureStore(history, initialState)

//     // ReactDOM.render(

//     const app = <Provider store={store}>
//     <ConnectedRouter history={history}>
// <ThemeProvider theme={light}>
//         <Router history={history}>
//           <RecordView

//             // type={'commercecategory'} id="44"
//             dispatch={
//               fetchRecord => fetchRecord
//               // (a: AnyAction)=>getApplicationStore().dispatch(fetchRecord(a as any)
//               // {fetchRecord: (c: FetchRecordOptions) => dispatch(fetchRecord(c))}
//             }
//           />
//         </Router>
//       </ThemeProvider>

//       </ConnectedRouter>
//     </Provider>

// const wrapper = mount(app)
//   wrapper.setState({type: 'commercecategory', id: 1})
// wrapper.update()
//     console.log(wrapper.html(), wrapper.getDOMNode().ownerDocument!.location.href);


//   });
});
