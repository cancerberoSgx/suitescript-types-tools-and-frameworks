// mkdirp : create a folder hierarchy even if parents don't exists
// rm -rf foo : remove folders even if they contain files or even other folders
// mv foo bar  : move folders are all its descendants
// cp -r foo bar: copy folder structures even if they contain sub files and sub folders
