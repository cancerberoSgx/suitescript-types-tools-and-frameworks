// This file is automatically generated - don't edit it. 
// Search Types for record Tax Detail (taxdetail)

// Search Columns

/** Tax Detail (taxdetail) Search Columns definition */
export interface taxdetailSearchColumn {
  /** Account (account: text) */
      account?: string;
  /** Details (details: textarea) */
      details?: string;
  /** Line Number (linenumber: integer) */
      linenumber?: number;
  /** Tax Amount (taxamount: currency) */
      taxamount?: number;
  /** Tax Basis (Foreign Currency) (taxbasis: currency) */
      taxbasis?: number;
  /** Tax Code (taxcode: select) */
      taxcode?: string;
  /** Tax Amount (Foreign Currency) (taxfxamount: currency) */
      taxfxamount?: number;
  /** Tax Rate (taxrate: percent) */
      taxrate?: number;
  /** Tax Type (taxtype: select) */
      taxtype?: string;
}


// Search Filters

/** Tax Detail (taxdetail) Search Filters definition */
export interface taxdetailSearchFilter {
/** Account (account: select) */
  account?: string;
  /** Line Number (linenumber: integer) */
  linenumber?: number;
  /** Tax Amount (taxamount: currency) */
  taxamount?: number;
  /** Tax Basis (Foreign Currency) (taxbasis: currency) */
  taxbasis?: number;
  /** Tax Code (taxcode: select) */
  taxcode?: string;
  /** Tax Rate (taxrate: percent) */
  taxrate?: number;
  /** Tax Type (taxtype: select) */
  taxtype?: string;
}


// Search Joins

/** Tax Detail (taxdetail) Search Joins definitions. */
export interface taxdetailSearchJoinTypes {

}


// Search Filter Types

/** Tax Detail (taxdetail) Search Filter Type definitions. */
export interface taxdetailSearchFilterTypes {
account: 'select';
  linenumber: 'integer';
  taxamount: 'currency';
  taxbasis: 'currency';
  taxcode: 'select';
  taxrate: 'percent';
  taxtype: 'select';
}


// Search Column Types

/** Tax Detail (taxdetail) Search Column Type definitions. */
export interface taxdetailSearchColumnTypes {
account: 'text';
  details: 'textarea';
  linenumber: 'integer';
  taxamount: 'currency';
  taxbasis: 'currency';
  taxcode: 'select';
  taxfxamount: 'currency';
  taxrate: 'percent';
  taxtype: 'select';
}