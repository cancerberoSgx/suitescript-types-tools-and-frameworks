// This file is automatically generated - don't edit it. 
// Search Types for record Workplace (workplace)

// Search Columns

/** Workplace (workplace) Search Columns definition */
export interface workplaceSearchColumn {
  /** City (city: text) */
      city?: string;
  /** Country (country: text) */
      country?: string;
  /** Internal ID (internalid: select) */
      internalid?: string;
  /** Inactive (isinactive: checkbox) */
      isinactive?: boolean;
  /** Name (name: text) */
      name?: string;
  /** Name (no hierarchy) (namenohierarchy: text) */
      namenohierarchy?: string;
  /** Phone (phone: phone) */
      phone?: string;
  /** State/Province (state: text) */
      state?: string;
  /** Zip (zip: text) */
      zip?: string;
}


// Search Filters

/** Workplace (workplace) Search Filters definition */
export interface workplaceSearchFilter {
/** Address (address: textarea) */
  address?: string;
  /** City (city: text) */
  city?: string;
  /** Country (country: select) */
  country?: string;
  /** County/State (county: text) */
  county?: string;
  /** Internal ID (internalid: select) */
  internalid?: string;
  /** Internal ID (Number) (internalidnumber: integer) */
  internalidnumber?: string;
  /** Inactive (isinactive: checkbox) */
  isinactive?: boolean;
  /** Name (name: text) */
  name?: string;
  /** Phone (phone: phone) */
  phone?: string;
  /** State/Province (state: select) */
  state?: string;
  /** Zip (zip: text) */
  zip?: string;
}


// Search Joins

/** Workplace (workplace) Search Joins definitions. */
export interface workplaceSearchJoinTypes {
/** workplace's address search filter. Address. Actual name: Address. */
  address: 'address';
  /** workplace's time search filter. Time. Actual name: Time. */
  time: 'time';
  /** workplace's timeentry search filter. Time Entry. Actual name: Time Entry. */
  timeentry: 'timeentry';
  /** workplace's user search filter. User. Actual name: Employee. */
  user: 'user';
}


// Search Filter Types

/** Workplace (workplace) Search Filter Type definitions. */
export interface workplaceSearchFilterTypes {
address: 'textarea';
  city: 'text';
  country: 'select';
  county: 'text';
  internalid: 'select';
  internalidnumber: 'integer';
  isinactive: 'checkbox';
  name: 'text';
  phone: 'phone';
  state: 'select';
  zip: 'text';
}


// Search Column Types

/** Workplace (workplace) Search Column Type definitions. */
export interface workplaceSearchColumnTypes {
city: 'text';
  country: 'text';
  internalid: 'select';
  isinactive: 'checkbox';
  name: 'text';
  namenohierarchy: 'text';
  phone: 'phone';
  state: 'text';
  zip: 'text';
}